{
    "name": "@aquarium-wars/whale",
    "version": "1.0.0",
    "license": "UNLICENSED",
    "main": "dist/index.js",
    "types": "dist/index.d.ts",
    "devDependencies": {
      "@types/node": "^17.0.19",
      "prisma": "^3.10.0",
      "ts-node": "^10.5.0",
      "typescript": "^4.5.5"
    },
    "scripts": {
      "build": "tsc --build",
      "migrate": "yarn prisma migrate deploy",
      "generate": "yarn prisma generate",
      "gen+build": "yarn prisma generate && tsc --build",
      "gen+build+migrate": "yarn gen+build && yarn migrate"
    },
    "dependencies": {
      "@prisma/client": "^3.10.0"
    }
  }
  